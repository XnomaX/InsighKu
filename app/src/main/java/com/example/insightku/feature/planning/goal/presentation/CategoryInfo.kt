package com.example.insightku.feature.planning.goal.presentation

/**
 * Lightweight category info for the SPENDING_CATEGORY selector in AutoAllocationDialog.
 * Carries both id (for storage/matching) and name (for display).
 */
data class CategoryInfo(
    val id: String,
    val name: String
)
