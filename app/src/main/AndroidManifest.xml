<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet permission for API calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Camera permission for receipt scanning -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    
    <!-- Storage permission for receipt images -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- For Android 13+ (API 33+), handle selected photos access in runtime permission -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <!-- Notification permission for budget alerts -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    
    <!-- Biometric permission for authentication -->
    <!-- <uses-permission android:name="android.permission.USE_FINGERPRINT" /> -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:name=".InsightKuApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InsightKu"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:localeConfig="@xml/locales_config"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.InsightKu"
            android:windowSoftInputMode="adjustResize"
            android:resizeableActivity="true"
            android:configChanges="smallestScreenSize|screenSize|screenLayout|orientation|density"
            android:enableOnBackInvokedCallback="false">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link from bank notification → AddTransaction pre-filled -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="insightku" android:host="add-transaction" />
            </intent-filter>
        </activity>
        
        <!-- File Provider for camera/gallery access -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Receiver for deleting auto-created transactions from notification -->
        <receiver
            android:name=".core.worker.DeleteTransactionReceiver"
            android:exported="false" />

        <!-- Receiver for dismissing bank notification without action -->
        <receiver
            android:name=".core.notification.DismissNotificationReceiver"
            android:exported="false" />

        <!-- Restart NotificationListenerService after device reboot -->
        <receiver
            android:name=".core.notification.NotificationListenerRestartReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Keepalive foreground service — keeps process alive so NotificationListenerService survives swipe -->
        <service
            android:name=".core.notification.NotificationListenerKeepaliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- NotificationListenerService for bank/e-wallet transaction detection -->
        <service
            android:name=".core.notification.BankNotificationListenerService"
            android:exported="true"
            android:label="@string/app_name"
            android:directBootAware="true"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Metadata holder (not a real service) required by the AppCompat
             per-app-language backport on API < 33 to store/apply locales. -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>
    </application>

</manifest>